// export const queryText = (element: HTMLElement | Element, selector: string) =>
//   element.querySelector(selector)?.textContent?.trim() || null;
// export const queryAttr = (
//   element: HTMLElement | Element,
//   selector: string,
//   attr: string,
// ) => element.querySelector(selector)?.getAttribute(attr) || null;

// export const queryImg = (element: HTMLElement | Element, selector: string) => {
//   const img = queryAttr(element, selector, "src");
//   return img ? `https://liquipedia.net${img}` : null;
// };

export {};
